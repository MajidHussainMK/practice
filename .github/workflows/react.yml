name: React CI/CD

on:
  push:
    branches: [ master, main ]

jobs:
  staging:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]

    steps:
    - uses: actions/checkout@v2
    
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    # - run: npm i
    # - run: npm test
    # - run: npm run build
    - run: echo This should run when code is pushed on master

  production:
    needs: staging
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]

    steps:
    - uses: actions/checkout@v2
    
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - run: echo ${{github.ref}}

    -name: Random name for step
    -run: echo MAJID
